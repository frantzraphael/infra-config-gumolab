#!/bin/bash

# Colors
GREEN="\e[32m"
RED="\e[31m"
CYAN="\e[36m"
GRAY="\e[90m"
RESET="\e[0m"

HOST=$(hostname)
UPTIME=$(uptime -p)
LOAD=$(awk '{print $1}' /proc/loadavg)

MEM_USED=$(free -h | awk '/Mem:/ {print $3}')
MEM_TOTAL=$(free -h | awk '/Mem:/ {print $2}')

DISK_USED=$(df -h / | awk 'NR==2 {print $3}')
DISK_TOTAL=$(df -h / | awk 'NR==2 {print $2}')
TEMP=$(sensors 2>/dev/null | awk '/Package id 0:/ {print $4}')
IP=$(hostname -I | awk '{print $1}')

check_service() {
  systemctl is-active --quiet "$1" && \
    echo -e "${GREEN}🟢 $1 running${RESET}" || \
    echo -e "${RED}🔴 $1 stopped${RESET}"
}

echo -e "${CYAN}🖥  $HOST${RESET}"
echo -e "${GRAY}────────────────────────────${RESET}"
echo -e "⏱  Uptime: $UPTIME"
echo -e "🔥 CPU Load: $LOAD"
echo -e "🌡  CPU Temp: ${TEMP:-N/A}"
echo -e "🧠 RAM: $MEM_USED / $MEM_TOTAL"
echo -e "💾 Disk (/): $DISK_USED / $DISK_TOTAL"
echo -e "🌐 IP: $IP"
echo
echo -e "${CYAN}🧩 Services${RESET}"
echo -e "${GRAY}────────────────────────────${RESET}"

check_service cloudflared
check_service jellyfin
check_service casaos

free -h | awk '/Swap:/ {print $3 "/" $2}'

